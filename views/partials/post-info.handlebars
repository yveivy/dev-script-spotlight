{{!-- <div class="card">

    <div class="card-header">
       <h4><a class="link" href="/post/{{id}}">{{title}}</a></h4>
       <p>Posted by {{user.username}} on {{created_at}}</p>
    </div>
    <p class="card-body">{{body}}</p>
</div> --}}

<!-- post-info.handlebars -->
<div class="post">
  <h2>{{title}}</h2>
  <p>{{body}}</p>
  <p>Posted by {{user.username}} on {{created_at}}</p>
</div>
{{!-- I would like to display grid but not sure if I can get it to work --}}

